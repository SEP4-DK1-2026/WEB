# This project uses a compose-based devcontainer setup.
# The active config is in .devcontainer/devcontainer.json and
# .devcontainer/compose.devcontainer.yaml.

FROM node:24.13.0-alpine

WORKDIR /usr/src/app
